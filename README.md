# Velora

A Lisbon perfume atelier frontend — React, Vite, and Tailwind CSS. No backend required.

## Run locally

```bash
npm install
npm run dev
```

Then open the URL shown in the terminal (typically `http://localhost:5173`).
